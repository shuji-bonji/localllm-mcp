# neko8 の LiteLLM (OpenAI 互換) ベースURL。末尾に /v1 は付けない
LOCALLLM_BASE=http://neko8.local:4000

# LiteLLM に master key を設定している場合のみ（未設定可）
# LOCALLLM_API_KEY=

# リクエストタイムアウト(ms)。省略時 120000
# LOCALLLM_TIMEOUT_MS=120000
